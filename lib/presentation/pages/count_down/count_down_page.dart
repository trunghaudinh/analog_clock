import 'package:flutter/cupertino.dart';

class CountDownPage extends StatelessWidget {
  const CountDownPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("count down page"),);
  }
}

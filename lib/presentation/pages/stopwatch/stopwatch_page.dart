import 'package:flutter/cupertino.dart';

class StopWatchPage extends StatelessWidget {
  const StopWatchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("stop watch page"),);
  }
}

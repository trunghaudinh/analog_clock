import 'package:flutter/cupertino.dart';

class GlobalPage extends StatelessWidget {
  const GlobalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Global Page"),);
  }
}

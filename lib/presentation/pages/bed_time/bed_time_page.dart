import 'package:flutter/cupertino.dart';

class BedTimePage extends StatelessWidget {
  const BedTimePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(child: Text("Bed time"),);
  }
}
